type SavedPost {
  id: ID!
  user: User!
  post: Post!
  createdAt: Time
  updatedAt: Time
}

extend type Query {
  getSavedPosts(input: PaginationInput): Posts
}

extend type Mutation {
  savePost(postId: ID!): Post
}
