# Where are all the schema files located? globs are supported eg  src/**/*.graphqls
schema:
  - graph/schema/*.graphqls

# Where should the generated server code go?
exec:
  filename: graph/generated/generated.go
  package: generated

# Uncomment to enable federation
# federation:
#   filename: graph/generated/federation.go
#   package: generated

# Where should any generated models go?
model:
  filename: graph/model/models_gen.go

# Where should the resolver implementations go?
resolver:
  layout: follow-schema
  dir: graph/resolver
  package: graph

# Optional: turn on use `gqlgen:"fieldName"` tags in your models
# struct_tag: json

# Optional: turn on to use []Thing instead of []*Thing
# omit_slice_element_pointers: false

# Optional: set to speed up generation time by not performing a final validation pass.
# skip_validation: true

# gqlgen will search for any type names in the schema in these go packages
# if they match it will use them, otherwise it will generate them.
autobind: []

# This section declares type mapping between the GraphQL and go type systems
#
# The first line in each type will be used as defaults for resolver arguments and
# modelgen, the others will be allowed when binding to fields. Configure them to
# your liking
models:
  User:
    model: github.com/plogto/core/graph/model.User
    fields:
      connectionStatus:
        resolver: true
      followingCount:
        resolver: true
      followersCount:
        resolver: true
      followRequestsCount:
        resolver: true
      postsCount:
        resolver: true
  Post:
    model: github.com/plogto/core/graph/model.Post
    fields:
      user:
        resolver: true
      likes:
        resolver: true
      replies:
        resolver: true
      isLiked:
        resolver: true
      isSaved:
        resolver: true
  File:
    model: github.com/plogto/core/graph/model.File
  Connection:
    model: github.com/plogto/core/graph/model.Connection
    fields:
      following:
        resolver: true
      follower:
        resolver: true
  Tag:
    model: github.com/plogto/core/graph/model.Tag
  PostLike:
    model: github.com/plogto/core/graph/model.PostLike
    fields:
      user:
        resolver: true
      post:
        resolver: true
  PostSave:
    model: github.com/plogto/core/graph/model.PostSave
    fields:
      user:
        resolver: true
      post:
        resolver: true
  OnlineUser:
    model: github.com/plogto/core/graph/model.OnlineUser
    fields:
      user:
        resolver: true
  Notification:
    model: github.com/plogto/core/graph/model.Notification
    fields:
      notificationType:
        resolver: true
      sender:
        resolver: true
      receiver:
        resolver: true
      post:
        resolver: true
      reply:
        resolver: true
  NotificationType:
    model: github.com/plogto/core/graph/model.NotificationType
